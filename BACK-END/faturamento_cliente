SELECT 
    c.nome AS cliente,
    COUNT(p.id) AS total_pedidos_feitos,
    SUM(p.total) AS total_gasto_na_loja
FROM clientes c
INNER JOIN pedidos p ON c.id = p.cliente_id
GROUP BY c.id
ORDER BY total_gasto_na_loja DESC;
